server:
  port: ${PORT:8001} # use $PORT if set, fallback to 8761 locally

spring:
  application:
    name: api-gateway
#lb://product-service
  cloud:
    gateway:
      routes:
        - id: product-service
          uri: https://product-service-ijnr.onrender.com
          predicates:
            - Path=/products/**
          filters:
            - StripPrefix=0

eureka:
  client:
    serviceUrl:
      defaultZone: https://eureka-service-8vz8.onrender.com/eureka/
    register-with-eureka: true
    fetch-registry: true

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    gateway:
      enabled: true

logging:
  level:
    com.netflix.discovery: DEBUG
    org.springframework.cloud.gateway: DEBUG