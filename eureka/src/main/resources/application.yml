server:
  port: ${PORT:${DISCOVERY_PORT}}

spring:
  application:
    name: ${DISCOVERY_NAME}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com.netflix.discovery: DEBUG
